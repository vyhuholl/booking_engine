Сгенерируй новый эндпоинт для booking engine.

Аргумент: $ARGUMENTS

Создай:
1. Handler в internal/handler/ — парсинг запроса, вызов сервиса, формат ответа
2. Метод в BookingService (или новый сервис если нужно) — бизнес-логика
3. Метод в Repository — SQL-запрос
4. Тесты: unit для сервиса (table-driven, с testutil), интеграционный для репозитория
5. Регистрацию маршрута в роутере

Следуй конвенциям из CLAUDE.md. Используй testutil для фикстур.