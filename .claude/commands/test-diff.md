Посмотри, какие файлы изменились (git diff HEAD --name-only).
Для каждого изменённого .go файла определи, какой пакет затронут.
Запусти go test для затронутых пакетов с флагом -v -count=1.
Если тесты упали — покажи, какой тест и почему.